<html>Which property matches this object?//An object has different properties. A property of an object can tell you how it looks, feels, tastes, or smells. Properties can also tell you how an object will behave when something happens to it.//Thinks about each property. A soft object changes shape when pressed or squeezed. The tree bark is not soft. A bumpy object is covered in lumps and bumps. The tree bark is bumpy.//Soft//Bumpy// //Bumpy</html>
<html>Which property do these two objects have in common?//An object has different properties. A property of an object can tell you how it looks, feels, tastes, or smells. Different objects can have the same properties. You can use these properties to put objects into groups.//Look at each object. For each object, decide if it has that property. A bouncy object will bounce back from the floor if you drop it. Both objects are bouncy. Yellow is a color.  This color is yellow. The spring is not yellow. The property that both objects have in common is bouncy.//Bouncy//yellow// //Bouncy</html>
<html>Which property do these three objects have in common?//An object has different properties. A property of an object can tell you how it looks, feels, tastes, or smells. Properties can also tell you how an object will behave when something happens to it. Different objects can have properties in common. You can use these properties to put objects into groups.//For each object, decide if it has that property. A slippery object is hard to hold onto or stand on. None of the objects are slippery. A translucent object lets light through. But you cannot see clearly through a translucent object. The fries and the cracker are not translucent. Potato chips have a salty taste. All three objects are salty. The property that all three objects have in common is salty.//Slippery//Translucent//Salty//Salty</html>
<html>Which is smoother?//Every object is made of one or more materials. A material is a type of matter. Wood, glass, metal, and plastic are common materials. A material has different properties. A material's properties tell you how it looks, feels, tastes, or smells. Some examples of properties are shiny, hard, fragile, and stretchy. For example, a shiny material reflects a lot of light. A fragile material breaks when you drop it.//Smooth is a property. A smooth material is not rough or bumpy. Look at each picture, one at a time. Imagine touching the material shown in each picture. Of the choices, the nylon track suit is smoother. If you touch nylon fabric, it will not feel rough.//Nylon track suit//Bark// //Nylon track suit</html>
<html>Which property do these four objects have in common?//An object has different properties. A property of an object can tell you how it looks, feels, tastes, or smells. Properties can also tell you how an object will behave when something happens to it. Different objects can have properties in common. You can use these properties to put objects into groups. Grouping objects by their properties is called classification.//For each object, decide if it has that property. A sticky object can attach or stick to other things. The jello is sticky, but the marbles are not. A translucent object lets light through. But you cannot see clearly through a translucent object. All four objects are translucent. Sugar has a sweet taste. The jello is sweet, but the marbles and the icicle are not. The property that all four objects have in common is translucent.//sticky//translucent//sweet//translucent</html>